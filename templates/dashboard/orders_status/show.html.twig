{% extends 'dashboard/base.html.twig' %}

{% block title %}OrdersStatus{% endblock %}

{% block body %}
    <h1>OrdersStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ orders_status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ orders_status.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ orders_status.code }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ orders_status.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('orders_status_index') }}">back to list</a>

    <a href="{{ path('orders_status_edit', {'id': orders_status.id}) }}">edit</a>

    {{ include('dashboard/orders_status/_delete_form.html.twig') }}
{% endblock %}
