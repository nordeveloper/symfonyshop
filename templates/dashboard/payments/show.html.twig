{% extends 'dashboard/base.html.twig' %}

{% block title %}Payments{% endblock %}

{% block body %}
    <h1>Payments</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ payment.id }}</td>
            </tr>
            <tr>
                <th>Active</th>
                <td>{{ payment.active }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ payment.title }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ payment.image }}</td>
            </tr>
            <tr>
                <th>Action_url</th>
                <td>{{ payment.actionUrl }}</td>
            </tr>
            <tr>
                <th>Merchant_account</th>
                <td>{{ payment.merchantAccount }}</td>
            </tr>
            <tr>
                <th>Merchant_password</th>
                <td>{{ payment.merchantPassword }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dashboard_payment_index') }}">back to list</a>

    <a href="{{ path('dashboard_payment_edit', {'id': payment.id}) }}">edit</a>

    {{ include('dashboard/payments/_delete_form.html.twig') }}
{% endblock %}
