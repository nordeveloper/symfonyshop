{% extends 'dashboard/base.html.twig' %}

{% block title %}Edit Articles{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-title">
            <div class="col-12">
            <h4>Edit Articles</h4>
            <a href="{{ path('dashboard_articles_index') }}">back to list</a>
            </div>
        </div>
    </div>


    {{ include('dashboard/articles/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('dashboard/articles/_delete_form.html.twig') }}
{% endblock %}
